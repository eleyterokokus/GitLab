Заключение и выводы

Попарное распределение:

Признаки Income и Loan могут имеют линейную зависимость. Возраст (Age) слабо коррелируется с целевой переменной.

Корреляция:

Признаки Income и Loan  сильно коррелированы между собой, что может потребовать удаления одного из них для избежания мультиколлинеарности.

Баланс классов:

Если классы несбалансированы (например, 90% не дефолт и 10% дефолт), это может потребовать применения методов балансировки (например, oversampling, undersampling или SMOTE).

Рекомендации:


Предполагается наличие более релевантной выборки.

Построить модель классификации для прогнозирования дефолта.